_schema-version: "3.2"
ID: ajaxtest
description: A Fiori application.
version: 0.0.1
modules:
- name: ajaxtest-dest-content
  type: com.sap.application.content
  requires:
  - name: ajaxtest-dest-srv
    parameters:
      content-target: true
  - name: ajaxtest-repo-host
    parameters:
      service-key:
        name: ajaxtest-repo-host-key
  - name: ajaxtest-uaa
    parameters:
      service-key:
        name: ajaxtest-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ajaxtest_repo_host
          ServiceInstanceName: ajaxtest-html5-srv
          ServiceKeyName: ajaxtest-repo-host-key
          sap.cloud.service: ajaxtest
        - Authentication: OAuth2UserTokenExchange
          Name: ajaxtest_uaa
          ServiceInstanceName: ajaxtest-xsuaa-srv
          ServiceKeyName: ajaxtest-uaa-key
          sap.cloud.service: ajaxtest
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: ajaxtest-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ajaxtest-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ajaxtest.zip
      name: ajaxtest
      target-path: resources/
- name: ajaxtest
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ajaxtest-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ajaxtest-dest-srv
    service-plan: lite
- name: ajaxtest-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ajaxtest-xsuaa-srv
    service-plan: application
- name: ajaxtest-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ajaxtest-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
